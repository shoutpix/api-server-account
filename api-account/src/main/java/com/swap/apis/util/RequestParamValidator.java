package com.swap.apis.util;


import org.springframework.util.StringUtils;

public class RequestParamValidator {
	/*
	 * public static void validateKeyMeta(KeyMeta keyMeta) { if
	 * (!StringUtils.hasLength(keyMeta.getKeyName())) { throw new
	 * KeySystemException(KeySystemExceptionType.INVALID_PARAMETER,
	 * "Key name must be not null"); }
	 * 
	 * if (keyMeta.getKeyType() == Type.NUMBER) { if (keyMeta.getMinLength() == null
	 * || keyMeta.getMinLength() < 0) { throw new
	 * KeySystemException(KeySystemExceptionType.INVALID_PARAMETER,
	 * "Number type key should have min-length"); }
	 * 
	 * if (keyMeta.getGeneratorType() == null) { throw new
	 * KeySystemException(KeySystemExceptionType.INVALID_PARAMETER,
	 * "Number type key should have generator"); } } }
	 */
}
